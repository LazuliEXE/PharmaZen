{# <div class="card">
    <img src="https://picsum.photos/200/100" class="card-img-top" alt=""/>
    <div class="card-body d-flex justify-content-center gap-3">
        <div class="card-title">
            <h3>{{medicament.nomComm}}</h3>
            <h3>{{medicament.nomGen}}</h3>
        </div>
        <div class="card-attributes">
            <h6>{{medicament.prix|format_currency('EUR', locale: 'fr')}}</h6>
            <a href="#" class="btn btn-success text-nowrap">Ajouter au panier</a>
        </div>
    </div>
</div> #}

<div class="card">
    <img src="https://picsum.photos/200/100" class="card-img-top" alt=""/>
    <div class="card-body">
        <!-- Ligne 1 : nom commercial + prix -->
        <div class="d-flex justify-content-between align-items-center mb-2">
            <h3 class="mb-0">{{ medicament.nomComm }}</h3>
            <h6 class="mb-0 text-end">{{ medicament.prix|format_currency('EUR', locale: 'fr') }}</h6>
        </div>
        <!-- Ligne 2 : nom générique + bouton -->
        <div class="d-flex justify-content-between align-items-center">
            <h3 class="mb-0">{{ medicament.nomGen }}</h3>
            <a href="#" class="btn btn-success text-nowrap">Ajouter au panier</a>
        </div>
    </div>
</div>